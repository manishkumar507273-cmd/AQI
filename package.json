{
  "name": "smart-airnet",
  "private": true,
  "version": "1.0.0",
  "description": "Smart AirNet — Real-Time AQI & Weather Telemetry Monitor",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
